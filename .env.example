# =============================================================================
# Environment Configuration Template
# =============================================================================
# Copy this file to .env and fill in your actual values
# cp .env.example .env

# =============================================================================
# Database Configuration
# =============================================================================
# PostgreSQL connection string
# Format: postgresql://username:password@host:port/database
# Local development (with docker-compose):
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/prop_analysis
# Railway (auto-provided, don't set manually):
# DATABASE_URL=${{Postgres.DATABASE_URL}}

# =============================================================================
# The Odds API Configuration (Optional)
# =============================================================================
# Get your API key from: https://the-odds-api.com/
# Note: Free tier does NOT include player props
# Required only if using historical data collection
ODDS_API_KEY=your_odds_api_key_here
ODDS_API_BASE_URL=https://api.the-odds-api.com/v4

# =============================================================================
# Scraping Configuration
# =============================================================================
# How often to scrape BettingPros (in minutes)
SCRAPE_INTERVAL_MINUTES=5

# Maximum concurrent HTTP requests
MAX_CONCURRENT_REQUESTS=5

# Delay between requests (seconds) - be respectful!
REQUEST_DELAY_MIN=1.0
REQUEST_DELAY_MAX=2.0

# =============================================================================
# Analysis Configuration
# =============================================================================
# Percentage threshold for significant line movement (e.g., 10 = 10%)
LINE_MOVEMENT_THRESHOLD_PCT=10.0

# Absolute yards threshold for significant line movement
LINE_MOVEMENT_THRESHOLD_ABS=5.0

# Hours before kickoff to consider for "late" movement
HOURS_BEFORE_KICKOFF_THRESHOLD=3.0

# =============================================================================
# Application Configuration
# =============================================================================
# Environment: development, staging, production
ENVIRONMENT=development

# Enable debug mode (true/false)
DEBUG=true

# =============================================================================
# Redis Configuration (Optional - for caching)
# =============================================================================
# Local development (with docker-compose):
# REDIS_URL=redis://localhost:6379/0
# Railway (auto-provided when you add Redis):
# REDIS_URL=${{Redis.REDIS_URL}}

# =============================================================================
# Frontend Configuration (for CORS)
# =============================================================================
# Allowed origins for CORS (comma-separated)
# Development:
# CORS_ORIGINS=http://localhost:3000,http://localhost:5173
# Production (Railway):
# CORS_ORIGINS=https://your-frontend-app.railway.app
